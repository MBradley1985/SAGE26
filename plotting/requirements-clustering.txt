# Python deps for plotting/mqg_clustering_bias.py
# Install with pip into a venv built on top of cluster MODULES (see
# plotting/setup_clustering_env.sh). Corrfunc is the only one that compiles
# (needs a C compiler + GSL from `module load`); the rest are pure wheels.
Corrfunc      # requires gcc + GSL (gsl-config on PATH) at install time
colossus
numpy
scipy
h5py
matplotlib
astropy
